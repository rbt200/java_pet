public class Avarage {
	// public static double averageValue(int N, int[] A) {
	// // this is default OUTPUT. You can change it.
	// int result = 0;

	// Arrays.sort(A);

	// // write your Logic here:
	// for (int i = 1; i < A.length - 1; i++) {
	// result += A[i];
	// }

	// return Double.valueOf(result) / (A.length - 2);
	// }

        // System.out.println("Hello world!");
        // int[] intArray = new int[] { 2, 3, 1, 4, 5, 2 };

        // System.out.println(averageValue(6, intArray));}
